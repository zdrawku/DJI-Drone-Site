export interface DjiDronesComparisonType {
  model: string;
  takeoff_weight_g: number;
  dimensions_mm: string;
  ascent_speed_m_s: number;
  takeoff_altitude_m: number;
  flight_time_min: number;
  flight_distance_km: number;
  operating_temperature_c: string;
  camera: string;
}
